function hasTwoCubeSums(n) {
	const count = {};
    let result = 0;
    n.toLowerCase().split('').forEach(char => {
        count[char] = (count[char] || 0) + 1;
    });
    for (let char in count) {
        if (count[char] > 1) result++;
    }
    return result;
}
